import { FilterpublishPipe } from './filterpublish.pipe';

describe('FilterpublishPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpublishPipe();
    expect(pipe).toBeTruthy();
  });
});
