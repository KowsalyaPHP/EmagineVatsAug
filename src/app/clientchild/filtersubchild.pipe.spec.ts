import { FiltersubchildPipe } from './filtersubchild.pipe';

describe('FiltersubchildPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltersubchildPipe();
    expect(pipe).toBeTruthy();
  });
});
