import { FilterchildPipe } from './filterchild.pipe';

describe('FilterchildPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterchildPipe();
    expect(pipe).toBeTruthy();
  });
});
