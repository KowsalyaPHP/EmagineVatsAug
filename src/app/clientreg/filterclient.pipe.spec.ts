import { FilterclientPipe } from './filterclient.pipe';

describe('FilterclientPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterclientPipe();
    expect(pipe).toBeTruthy();
  });
});
