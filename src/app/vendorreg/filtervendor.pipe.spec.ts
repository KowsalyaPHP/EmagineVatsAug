import { FiltervendorPipe } from './filtervendor.pipe';

describe('FiltervendorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltervendorPipe();
    expect(pipe).toBeTruthy();
  });
});
